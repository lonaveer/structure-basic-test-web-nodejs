{
  "scripts": {
    "test": "jest --config=jest.config.unit.ts",
    "test:watch": "jest --config=jest.config.unit.ts --watch",
    "test:coverage": "jest --config=jest.config.unit.ts --coverage",
    "itest": "jest --config=jest.config.integration.ts",
    "itest:watch": "jest --config=jest.config.integration.ts --watch",
    "itest:coverage": "jest --config=jest.config.integration.ts --coverage",
    "alltest": "jest",
    "alltest:watch": "jest --watch",
    "alltest:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "ts-node": "^10.9.2"
  }
}
